{
    "$schema": "http://json-schema.org/schema",
    "$id": "Schema",
    "title": "schema",
    "type": "object",
    "properties": {
        "nombre": {
            "type": "string",
            "description": "Nombre que se le asignará al módulo"
        },
        "ruta": {
            "type": "string",
            "description": "Ruta donde se generará la carpeta base del módulo"
        },
        "additionalProperties": false
    },
    "required": []
}