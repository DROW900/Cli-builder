{
    "$schema": "http://json-schema.org/schema",
    "$id": "Schema",
    "title": "schema",
    "type": "object",
    "properties": {
        "nombre": {
            "type": "string",
            "description": "Nombre del componente a generar"
        },
        "direccion": {
            "type": "string",
            "description": "Url donde se generará la carpeta"
        },
        "modulo": {
            "type": "string",
            "description": "Ruta del módulo donde se va a declarar el import del nuevo componente"
        },
        "columnas": {
            "type": "number",
            "description": "Número de columnas que serán generadas en la tabla"
        },
        "numeroComponentes": {
            "type": "number",
            "description": "Número de componentes que se desean generar",
            "default": 1
        },
        "additionalProperties": false
    },
    "required": []
}